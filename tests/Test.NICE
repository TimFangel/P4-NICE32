setup {
    bool yes = true;

    component lamp {
        port: 5;
        protocol: digital;
        interval: 1500;
        direction: input;

        bool x = true;
    }
}

functions {
    int func coolestFunction(int x) {
        int c = 2 * 2;
    }
}

main {
    bool a = lamp.x;
    int b = coolestFunction(1 + 2 + 3);
}