setup {
    component bruh { 
        port: 2;
        protocol: digital;
        interval: 1000;
        direction: input;

        int a = 9;
    }
}

functions {
    int func x(int n) {
    if (n < 0) {
        n = n + 200;
    }
        return 5 * n + 2;
    }
}

main {
    int a = 5;
    int b = 2;
    int c = 7;
    int d = -a * (b + c);

    if (a > b) {
        a = a + 10;
    }

    bool bob = true;

    while (bob != false) {
        b = b - 1;
        if (b < 1) {
            bob = !bob;
        }
    }

    bob = true;
}