functions {
    int func x(int n) {
        int g = 20;
        return g;
    }
}

setup {
    
}

main {
    bool p = true;
    if (p) {
        p = false;
    }

    float fih = 2.2;
    float guh = -200.0;

    int hh = -10 + (int) (fih + guh) + a.a;
    int c = hh * 10 + gg - 5;

    int gug = x(23) + x(2);
}